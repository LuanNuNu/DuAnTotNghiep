<Window x:Class="DuAn_QuanLiKhachSan.PageChild.thongtinPhong"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DuAn_QuanLiKhachSan.PageChild"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="thongtinPhong"
       Height="640" Width="1180" WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Border Background="{DynamicResource SecundaryBackgroundColor}"  CornerRadius="20" BorderThickness="2" BorderBrush="#7f7cf1">
        <Grid Loaded="Load">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="150"/>
                <RowDefinition Height="300"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,8,0" Height="30" Orientation="Horizontal" FlowDirection="RightToLeft">
                <Button x:Name="btnClose" Style="{StaticResource IconButtonsStyle}" Content="{DynamicResource close}" Click="btnClose_Click" Tag="IsCloseButton"/>
                <Button x:Name="btnMinimize" Style="{DynamicResource IconButtonsStyle}" Content="{DynamicResource minimize}" Click="btnMinimize_Click"/>
            </StackPanel>

            <TextBlock x:Name="tenPhong" Grid.Row="1" Text="TÊN PHÒNG" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextColor}"/>
            <Grid  Grid.Row="2" Margin="40 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Vertical" Margin="0 0 0 6">
                        <TextBlock Text="KHÁCH HÀNG:" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryTextColor}"/>
                        <TextBlock x:Name="tenKh" FontSize="16" Foreground="{DynamicResource PrimaryTextColor}"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="CHECKIN:" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryTextColor}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="ngayCheckIn" FontSize="16" Foreground="{DynamicResource PrimaryTextColor}"/>
                            <TextBlock x:Name="gioCheckIn" FontSize="16" Foreground="{DynamicResource PrimaryTextColor}" Margin="10 0 0 0"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="CHECKOUT:" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryTextColor}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="ngayCheckOut" FontSize="16" Foreground="{DynamicResource PrimaryTextColor}"/>
                            <TextBlock x:Name="gioCheckOut" FontSize="16" Foreground="{DynamicResource PrimaryTextColor}" Margin="10 0 0 0"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Vertical" Grid.Column="1">
                    <StackPanel Orientation="Vertical" Margin="0 0 0 6">
                        <TextBlock Text="THỜI GIAN THUÊ:" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryTextColor}"/>
                        <StackPanel Orientation="Horizontal" >
                            <materialDesign:PackIcon x:Name="icDayorHour" Width="25" Height="25"/>
                            <TextBlock x:Name="thoigGianThueP" FontSize="16" Foreground="{DynamicResource PrimaryTextColor}" VerticalAlignment="Center"/>
                            <Label FontSize="16" Foreground="{DynamicResource PrimaryTextColor}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="SỐ NGƯỜI:" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryTextColor}"/>
                        <StackPanel Orientation="Horizontal" >
                            <TextBlock x:Name="slNguoi"  FontSize="16" Foreground="{DynamicResource PrimaryTextColor}" VerticalAlignment="Center"/>
                            <Label Content="người" FontSize="16" Foreground="{DynamicResource PrimaryTextColor}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            <StackPanel Orientation="Vertical" Margin="20 0" Grid.Column="2" Grid.ColumnSpan="2">
                    <StackPanel Orientation="Vertical">
                        <Label Content="TÌNH TRẠNG PHÒNG:" Foreground="{DynamicResource PrimaryTextColor}" FontWeight="Bold" FontSize="14"/>
                        <TextBlock x:Name="tinhTrangPhong" HorizontalAlignment="Left" Width="250" FontSize="15" Height="32" Foreground="{DynamicResource PrimaryTextColor}">
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Vertical">
                        <Label Content="Cập nhật tình trạng dọn dẹp" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextColor}"/>
                        <ComboBox x:Name="tinhTrangDonDep" HorizontalAlignment="Left" Width="250" FontSize="15" Height="32" Foreground="{DynamicResource PrimaryTextColor}" Style="{DynamicResource ComboBoxFlatStyle}">
                            <ComboBoxItem Content="Đã dọn dẹp"/>
                            <ComboBoxItem Content="Chưa dọn dẹp"/>
                            <ComboBoxItem Content="Đang sửa chữa"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="3" Margin="20 0">
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical">
                    <Separator Margin="20 20" Background="{DynamicResource TertiaryBlackColor}"/>
                    <TextBlock Text="Thêm dịch vụ phòng" Margin="0 10" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource PrimaryTextColor}"/>
                    <StackPanel Orientation="Vertical" Margin="4 0">
                        <StackPanel Orientation="Horizontal">
                            <Label Content="TÊN DỊCH VỤ" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextColor}"/>
                            <fa:IconImage Icon="BellConcierge" Style="{DynamicResource Icon}"/>
                        </StackPanel>
                        <ComboBox x:Name="listDichVụ" HorizontalAlignment="Left" Width="220" FontSize="15" Height="32" Foreground="{DynamicResource PrimaryTextColor}" Style="{DynamicResource ComboBoxFlatStyle}" SelectionChanged="listDichVụ_SelectionChanged">
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="4 0">
                        <StackPanel Orientation="Horizontal">
                            <Label Content="ĐƠN GIÁ" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextColor}"/>
                            <fa:IconImage Icon="MoneyBill" Style="{DynamicResource Icon}"/>
                        </StackPanel>
                        <TextBox x:Name="giaDv" Style="{DynamicResource TextBoxStyle}" HorizontalAlignment="Left" Width="220" FontSize="15" Foreground="{DynamicResource PrimaryTextColor}" IsEnabled="False"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="4 0">
                        <StackPanel Orientation="Horizontal">
                            <Label Content="SỐ LƯỢNG" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextColor}"/>
                            <fa:IconImage Icon="Plus" Style="{DynamicResource Icon}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="cong" Style="{StaticResource gridEditButton}" Click="cong_Click">
                                <fa:IconImage Icon="Plus" Style="{StaticResource gridBtnIcon}"/>
                            </Button>
                            <TextBox x:Name="soLuong" Text="1" Margin="2 0"/>
                            <Button x:Name="tru" Style="{StaticResource gridEditButton}" Click="tru_Click">
                                <fa:IconImage Icon="Minus" Style="{StaticResource gridBtnIcon}"/>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                    <Button x:Name="btn_themDichVu" Content="Thêm dịch vụ" Style="{DynamicResource MainBtn}" Width="140" Click="btn_themDichVu_Click" Margin="0 14" />
                </StackPanel>
                
                <DataGrid x:Name="datagridDichVu"  Grid.Row="1" Grid.Column="1" Margin="10" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="MÃ DỊCH VỤ" Width="*" Binding="{Binding MaDV}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="TÊN DỊCH VỤ" Width="*" Binding="{Binding TenDV}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="SỐ LƯỢNG" Width="*" Binding="{Binding SoLuongDV}"/>
                        <DataGridTextColumn Header="ĐƠN GIÁ" Width="*" Binding="{Binding GiaTien , StringFormat={}{0:N0}}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="THÀNH TIỀN" Width="*" Binding="{Binding TongGiaTri , StringFormat={}{0:N0}}" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="TÙY CHỈNH" IsReadOnly="True" Width="auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource gridEditButton}" Margin="0 0 4 0" x:Name="btn_UpdateDichVu" Click="btn_UpdateDichVu_Click">
                                            <fa:IconImage Icon="Pencil" Style="{StaticResource gridBtnIcon}"/>
                                        </Button>
                                        <Button Style="{StaticResource gridRemoveButton}" x:Name="btn_delDichVu" Click="btn_delDichVu_Click">
                                            <fa:IconImage Icon="TrashCan" Style="{StaticResource gridBtnIcon}"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
             
                <Button x:Name="btn_NhanPhong" Content="Nhận phòng" Style="{DynamicResource MainBtn}" Grid.Column="3" Click="btn_NhanPhong_Click" />
                <Button x:Name="btn_ThanhToan" Content="Thanh toán" Style="{DynamicResource MainBtn}" Grid.Column="3" Visibility="Hidden" Click="btn_ThanhToan_Click"/>
                <Button x:Name="btn_CapNhat" Content="Cập nhật" Style="{DynamicResource MainBtn}" Grid.Column="4" Click="btn_CapNhat_Click"/>
  
            </Grid>

        </Grid>
    </Border>
</Window>
